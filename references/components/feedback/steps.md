# Steps
- 26px circle per step: completed = solid success-green + check glyph, current = solid brand + step number, upcoming = light neutral fill + muted number. The connecting line between two steps takes the color of the step it leads *out of* (green after a completed step, neutral gray otherwise) — never colors the line by the step it leads *into*.
