# Divider
- Plain 1px `--color-border-default`-family line for ordinary section separation (majority use case).
- Reserve the **labeled** variant (text centered between two line segments, e.g. "Hoặc") specifically for "alternative path" moments (OR between two options) — not as a fancier way to separate generic sections.
