# Popconfirm
- Compact card (≈240px), title with an optional leading `⚠` icon, one line of description, then the same left-cancel/right-confirm button order. `confirmText`/`cancelText` should stay short (1–2 words: "Xóa", "Hủy", "Xác nhận") since the popover itself is narrow.

---
