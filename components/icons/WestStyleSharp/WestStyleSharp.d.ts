import * as React from 'react';
/** Icon: WestStyleSharp. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface WestStyleSharpProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const WestStyleSharp: React.FC<WestStyleSharpProps>;
export { WestStyleSharp };
