import * as React from 'react';
/** Icon: CloseStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CloseStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CloseStyleRound: React.FC<CloseStyleRoundProps>;
export { CloseStyleRound };
