import * as React from 'react';
/** Icon: CheckboxStateSelected. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CheckboxStateSelectedProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CheckboxStateSelected: React.FC<CheckboxStateSelectedProps>;
export { CheckboxStateSelected };
