import * as React from 'react';
/** Icon: CalendarTodayStyleTwoTone. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CalendarTodayStyleTwoToneProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CalendarTodayStyleTwoTone: React.FC<CalendarTodayStyleTwoToneProps>;
export { CalendarTodayStyleTwoTone };
