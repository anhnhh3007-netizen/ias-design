import * as React from 'react';
/** Icon: EastStyleOutlined. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface EastStyleOutlinedProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const EastStyleOutlined: React.FC<EastStyleOutlinedProps>;
export { EastStyleOutlined };
