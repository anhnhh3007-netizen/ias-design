import * as React from 'react';
/** Icon: MoreVertStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface MoreVertStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const MoreVertStyleRound: React.FC<MoreVertStyleRoundProps>;
export { MoreVertStyleRound };
