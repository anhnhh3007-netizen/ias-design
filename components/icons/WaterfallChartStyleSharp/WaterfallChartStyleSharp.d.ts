import * as React from 'react';
/** Icon: WaterfallChartStyleSharp. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface WaterfallChartStyleSharpProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const WaterfallChartStyleSharp: React.FC<WaterfallChartStyleSharpProps>;
export { WaterfallChartStyleSharp };
