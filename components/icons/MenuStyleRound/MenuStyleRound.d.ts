import * as React from 'react';
/** Icon: MenuStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface MenuStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const MenuStyleRound: React.FC<MenuStyleRoundProps>;
export { MenuStyleRound };
