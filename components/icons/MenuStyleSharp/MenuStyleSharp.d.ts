import * as React from 'react';
/** Icon: MenuStyleSharp. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface MenuStyleSharpProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const MenuStyleSharp: React.FC<MenuStyleSharpProps>;
export { MenuStyleSharp };
