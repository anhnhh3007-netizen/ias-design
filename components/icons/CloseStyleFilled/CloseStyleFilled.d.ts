import * as React from 'react';
/** Icon: CloseStyleFilled. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CloseStyleFilledProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CloseStyleFilled: React.FC<CloseStyleFilledProps>;
export { CloseStyleFilled };
