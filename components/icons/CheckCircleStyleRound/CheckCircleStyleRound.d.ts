import * as React from 'react';
/** Icon: CheckCircleStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CheckCircleStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CheckCircleStyleRound: React.FC<CheckCircleStyleRoundProps>;
export { CheckCircleStyleRound };
