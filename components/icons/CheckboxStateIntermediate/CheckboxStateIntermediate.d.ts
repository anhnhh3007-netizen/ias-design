import * as React from 'react';
/** Icon: CheckboxStateIntermediate. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CheckboxStateIntermediateProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CheckboxStateIntermediate: React.FC<CheckboxStateIntermediateProps>;
export { CheckboxStateIntermediate };
