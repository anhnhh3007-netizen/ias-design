import * as React from 'react';
/** Icon: CalendarTodayStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CalendarTodayStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CalendarTodayStyleRound: React.FC<CalendarTodayStyleRoundProps>;
export { CalendarTodayStyleRound };
