import * as React from 'react';
/** Icon: ArrowUpwardStyleSharp. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface ArrowUpwardStyleSharpProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const ArrowUpwardStyleSharp: React.FC<ArrowUpwardStyleSharpProps>;
export { ArrowUpwardStyleSharp };
