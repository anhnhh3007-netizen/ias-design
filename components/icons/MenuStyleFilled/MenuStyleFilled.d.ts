import * as React from 'react';
/** Icon: MenuStyleFilled. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface MenuStyleFilledProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const MenuStyleFilled: React.FC<MenuStyleFilledProps>;
export { MenuStyleFilled };
