import * as React from 'react';
/** Icon: CalendarTodayStyleOutlined. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CalendarTodayStyleOutlinedProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CalendarTodayStyleOutlined: React.FC<CalendarTodayStyleOutlinedProps>;
export { CalendarTodayStyleOutlined };
