import * as React from 'react';
export interface BoltStyleRoundProps { size?: number; color?: string; className?: string; style?: React.CSSProperties; }
export declare function BoltStyleRound(props: BoltStyleRoundProps): React.ReactElement | null;
