import * as React from 'react';
export interface AlarmStyleRoundProps { size?: number; color?: string; className?: string; style?: React.CSSProperties; }
export declare function AlarmStyleRound(props: AlarmStyleRoundProps): React.ReactElement | null;
