import * as React from 'react';
/** Icon: HomeWorkStyleSharp. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface HomeWorkStyleSharpProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const HomeWorkStyleSharp: React.FC<HomeWorkStyleSharpProps>;
export { HomeWorkStyleSharp };
