import * as React from 'react';
/** Icon: CheckboxStateDeselected. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CheckboxStateDeselectedProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CheckboxStateDeselected: React.FC<CheckboxStateDeselectedProps>;
export { CheckboxStateDeselected };
