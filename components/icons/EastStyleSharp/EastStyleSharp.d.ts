import * as React from 'react';
/** Icon: EastStyleSharp. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface EastStyleSharpProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const EastStyleSharp: React.FC<EastStyleSharpProps>;
export { EastStyleSharp };
