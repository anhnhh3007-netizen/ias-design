import * as React from 'react';
/** Icon: CalendarTodayStyleFilled. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CalendarTodayStyleFilledProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CalendarTodayStyleFilled: React.FC<CalendarTodayStyleFilledProps>;
export { CalendarTodayStyleFilled };
