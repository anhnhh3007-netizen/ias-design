import * as React from 'react';
/** Icon: WestStyleOutlined. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface WestStyleOutlinedProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const WestStyleOutlined: React.FC<WestStyleOutlinedProps>;
export { WestStyleOutlined };
