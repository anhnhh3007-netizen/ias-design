import * as React from 'react';
/** Icon: ArrowUpwardStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface ArrowUpwardStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const ArrowUpwardStyleRound: React.FC<ArrowUpwardStyleRoundProps>;
export { ArrowUpwardStyleRound };
