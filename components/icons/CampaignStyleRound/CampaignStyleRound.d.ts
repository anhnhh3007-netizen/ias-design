import * as React from 'react';
/** Icon: CampaignStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CampaignStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CampaignStyleRound: React.FC<CampaignStyleRoundProps>;
export { CampaignStyleRound };
