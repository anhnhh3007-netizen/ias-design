import * as React from 'react';
/** Icon: CalendarTodayStyleSharp. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface CalendarTodayStyleSharpProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const CalendarTodayStyleSharp: React.FC<CalendarTodayStyleSharpProps>;
export { CalendarTodayStyleSharp };
