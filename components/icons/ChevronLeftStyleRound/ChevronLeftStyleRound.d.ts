import * as React from 'react';
/** Icon: ChevronLeftStyleRound. Reads SVG from window.iconData — load assets/icons/icon-data.js first. */
export interface ChevronLeftStyleRoundProps {
  size?: number;
  color?: string;
  className?: string;
  style?: React.CSSProperties;
}
declare const ChevronLeftStyleRound: React.FC<ChevronLeftStyleRoundProps>;
export { ChevronLeftStyleRound };
